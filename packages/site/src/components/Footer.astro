---
const currentYear = new Date().getFullYear();
---

<footer class="footer">
  <div class="container footer__container">
    <p class="footer__copyright">
      &copy; {currentYear} Rudiger's Evocation of Events
    </p>
    <nav class="footer__nav" aria-label="Footer navigation">
      <a href="/rss.xml" class="footer__link">RSS</a>
      <span class="footer__separator" aria-hidden="true">·</span>
      <a href="https://github.com/lexicone42/dndblog" class="footer__link" rel="noopener noreferrer">
        GitHub
      </a>
      <span class="footer__separator" aria-hidden="true">·</span>
      <span class="footer__tagline">
        Built with <a href="https://astro.build" class="footer__link" rel="noopener noreferrer">Astro</a>
      </span>
    </nav>
  </div>
</footer>

<style>
  .footer {
    border-top: 1px solid var(--color-border);
    height: var(--header-height);
    margin-top: auto;
    background-color: var(--color-bg-secondary);
    display: flex;
    align-items: center;
  }

  .footer__container {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    align-items: center;
    gap: var(--space-sm);
    width: 100%;
  }

  .footer__copyright {
    color: var(--color-text-secondary);
    font-size: var(--text-sm);
    margin: 0;
    font-family: var(--font-serif);
  }

  .footer__nav {
    display: flex;
    gap: var(--space-sm);
    align-items: center;
  }

  .footer__separator {
    color: var(--color-gold);
  }

  .footer__link {
    color: var(--color-text-secondary);
    text-decoration: none;
    font-size: var(--text-sm);
    transition: color var(--transition-fast);
  }

  .footer__link:hover {
    color: var(--color-gold);
    text-decoration: underline;
  }

  .footer__tagline {
    color: var(--color-text-muted);
    font-size: var(--text-sm);
    font-style: italic;
  }

  /* Stack vertically on mobile */
  @media (max-width: 640px) {
    .footer__container {
      justify-content: center;
      text-align: center;
    }
  }
</style>
