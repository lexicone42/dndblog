---
import BaseLayout from '@layouts/BaseLayout.astro';
---

<BaseLayout title="Rudiger's Evocation of Events">
  <div class="landing">
    <header class="landing__header">
      <h1 class="landing__title">Rudiger's Evocation of Events</h1>
      <p class="landing__subtitle">A chronicle of the Mawframe Campaign</p>
    </header>

    <nav class="landing__nav">
      <a href="/blog/welcome-to-the-adventure" class="nav-card nav-card--primary">
        <h2 class="nav-card__title">Enter the Chronicle</h2>
        <p class="nav-card__description">Six adventurers. A sealed primordial. A cult waiting for centuries. Start here to meet the party and learn what they're fighting.</p>
      </a>

      <a href="/sessions" class="nav-card">
        <h2 class="nav-card__title">Session Records</h2>
        <p class="nav-card__description">Ten sessions of investigation, combat, and narrow escapes. Read the full campaign from Phandalin to Sharn.</p>
      </a>

      <a href="/reference" class="nav-card">
        <h2 class="nav-card__title">World Reference</h2>
        <p class="nav-card__description">Characters, locations, items, enemies, and factions. Everything the party has encountered, cross-referenced.</p>
      </a>
    </nav>

    <footer class="landing__footer">
      <p class="footer-tagline">"Knowledge is the only weapon that doesn't break."</p>
    </footer>
  </div>
</BaseLayout>

<style>
  .landing {
    min-height: 75vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: var(--space-2xl);
  }

  .landing__header {
    text-align: center;
    margin-bottom: var(--space-2xl);
  }

  .landing__title {
    font-family: var(--font-display);
    font-size: clamp(2rem, 6vw, 3rem);
    color: var(--color-gold);
    margin: 0 0 var(--space-sm);
    line-height: 1.2;
  }

  .landing__subtitle {
    font-family: var(--font-serif);
    font-size: var(--text-lg);
    color: var(--color-text-secondary);
    font-style: italic;
    margin: 0;
  }

  .landing__nav {
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
    width: 100%;
    max-width: 500px;
    margin-bottom: var(--space-2xl);
  }

  @media (min-width: 900px) {
    .landing__nav {
      flex-direction: row;
      max-width: 1000px;
      gap: var(--space-xl);
    }

    .nav-card {
      flex: 1;
    }

    .nav-card--primary {
      flex: 1.2;
    }
  }

  .nav-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: var(--space-xl) var(--space-lg);
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    text-decoration: none;
    color: var(--color-text);
    transition: border-color 0.2s, transform 0.2s, box-shadow 0.2s;
  }

  .nav-card--primary {
    border-color: var(--color-gold);
    border-width: 2px;
  }

  .nav-card:hover {
    border-color: var(--color-gold);
    transform: translateY(-3px);
    box-shadow: 0 6px 16px rgba(0, 0, 0, 0.25);
  }

  .nav-card__title {
    font-family: var(--font-display);
    font-size: var(--text-xl);
    color: var(--color-text);
    margin: 0 0 var(--space-sm);
    transition: color 0.2s;
  }

  .nav-card:hover .nav-card__title {
    color: var(--color-gold);
  }

  .nav-card__description {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    margin: 0;
    line-height: 1.5;
  }

  .landing__footer {
    text-align: center;
    padding-top: var(--space-xl);
    border-top: 1px solid var(--color-border);
    max-width: 500px;
  }

  .footer-tagline {
    font-family: var(--font-serif);
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    font-style: italic;
    margin: 0;
  }

  @media (max-width: 640px) {
    .landing {
      padding: var(--space-xl);
    }
  }
</style>
