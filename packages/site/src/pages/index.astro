---
import BaseLayout from '@layouts/BaseLayout.astro';
---

<BaseLayout title="The Evocation of Events - Adventures & Campaigns">
  <div class="landing">
    <header class="landing__header">
      <h1 class="landing__title">Chronicles of the Mawframe</h1>
      <p class="landing__subtitle">A D&D Campaign Journal</p>
    </header>

    <nav class="landing__nav">
      <a href="/blog/welcome-to-the-adventure" class="nav-card">
        <div class="nav-card__icon">
          <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"></path>
            <path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"></path>
          </svg>
        </div>
        <h2 class="nav-card__title">Welcome</h2>
        <p class="nav-card__description">Begin your journey here. Learn about our campaign and meet the heroes.</p>
      </a>

      <a href="/sessions" class="nav-card">
        <div class="nav-card__icon">
          <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M14.5 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7.5L14.5 2z"></path>
            <polyline points="14 2 14 8 20 8"></polyline>
            <line x1="16" y1="13" x2="8" y2="13"></line>
            <line x1="16" y1="17" x2="8" y2="17"></line>
            <line x1="10" y1="9" x2="8" y2="9"></line>
          </svg>
        </div>
        <h2 class="nav-card__title">Session Chronicles</h2>
        <p class="nav-card__description">Read through every session from the beginning. Follow the adventure as it unfolds.</p>
      </a>

      <a href="/reference" class="nav-card">
        <div class="nav-card__icon">
          <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="12" cy="12" r="10"></circle>
            <line x1="12" y1="16" x2="12" y2="12"></line>
            <line x1="12" y1="8" x2="12.01" y2="8"></line>
          </svg>
        </div>
        <h2 class="nav-card__title">Campaign Reference</h2>
        <p class="nav-card__description">Characters, locations, factions, and lore. Your guide to the world.</p>
      </a>
    </nav>
  </div>
</BaseLayout>

<style>
  .landing {
    min-height: 80vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: var(--space-xl);
  }

  .landing__header {
    text-align: center;
    margin-bottom: var(--space-3xl);
  }

  .landing__title {
    font-family: var(--font-display);
    font-size: clamp(2.5rem, 8vw, 4rem);
    background: linear-gradient(135deg, var(--color-gold) 0%, var(--color-gold-light) 50%, var(--color-gold) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin: 0 0 var(--space-sm);
    line-height: 1.1;
  }

  .landing__subtitle {
    font-family: var(--font-serif);
    font-style: italic;
    font-size: var(--text-xl);
    color: var(--color-text-muted);
    margin: 0;
  }

  .landing__nav {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--space-xl);
    width: 100%;
    max-width: 1000px;
  }

  .nav-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: var(--space-2xl) var(--space-xl);
    background: var(--color-bg-secondary);
    border: 3px solid var(--color-border);
    border-radius: var(--radius-lg);
    text-decoration: none;
    color: var(--color-text);
    transition: all var(--transition-base);
  }

  .nav-card:hover {
    border-color: var(--color-gold);
    transform: translateY(-8px);
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.3);
  }

  .nav-card__icon {
    color: var(--color-gold);
    margin-bottom: var(--space-lg);
    transition: transform var(--transition-base);
  }

  .nav-card:hover .nav-card__icon {
    transform: scale(1.1);
  }

  .nav-card__title {
    font-family: var(--font-display);
    font-size: var(--text-2xl);
    color: var(--color-text);
    margin: 0 0 var(--space-sm);
    transition: color var(--transition-fast);
  }

  .nav-card:hover .nav-card__title {
    color: var(--color-gold);
  }

  .nav-card__description {
    font-size: var(--text-base);
    color: var(--color-text-muted);
    margin: 0;
    line-height: 1.6;
  }

  @media (max-width: 640px) {
    .landing {
      padding: var(--space-lg);
      min-height: auto;
    }

    .landing__nav {
      grid-template-columns: 1fr;
    }

    .nav-card {
      padding: var(--space-xl) var(--space-lg);
    }
  }
</style>
