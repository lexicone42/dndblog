---
import BaseLayout from '@layouts/BaseLayout.astro';
---

<BaseLayout title="Rudiger's Evocation of Events - Chronicles of the Mawframe">
  <div class="landing">
    <header class="landing__header">
      <p class="landing__whisper">you found this</p>
      <h1 class="landing__title">Chronicles of the Mawframe</h1>
      <p class="landing__subtitle">
        <span class="subtitle-static">Recovered writings from the Astralglades Expedition</span>
        <span class="subtitle-pulse">the ley remembers what you forget</span>
      </p>
    </header>

    <div class="landing__warning">
      <span class="warning-glyph">◇</span>
      <span class="warning-text">Something is being built in the North Woods. The trees scream when they cut them.</span>
      <span class="warning-glyph">◇</span>
    </div>

    <nav class="landing__nav">
      <a href="/blog/welcome-to-the-adventure" class="nav-card nav-card--active">
        <div class="nav-card__sigil">
          <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="12" cy="12" r="10" class="sigil-outer"/>
            <circle cx="12" cy="12" r="4" class="sigil-inner"/>
            <line x1="12" y1="2" x2="12" y2="6" class="sigil-ray"/>
            <line x1="12" y1="18" x2="12" y2="22" class="sigil-ray"/>
            <line x1="2" y1="12" x2="6" y2="12" class="sigil-ray"/>
            <line x1="18" y1="12" x2="22" y2="12" class="sigil-ray"/>
          </svg>
        </div>
        <h2 class="nav-card__title">The Beginning</h2>
        <p class="nav-card__description">
          A bell inscribed with a name that should not be spoken.
          <em>Start here, if you dare.</em>
        </p>
        <span class="nav-card__fragment">session zero / origin point</span>
      </a>

      <a href="/sessions" class="nav-card">
        <div class="nav-card__sigil">
          <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M4 19.5A2.5 2.5 0 0 1 6.5 17H20"/>
            <path d="M6.5 2H20v20H6.5A2.5 2.5 0 0 1 4 19.5v-15A2.5 2.5 0 0 1 6.5 2z"/>
            <line x1="8" y1="7" x2="16" y2="7" class="page-line"/>
            <line x1="8" y1="11" x2="14" y2="11" class="page-line"/>
            <line x1="8" y1="15" x2="12" y2="15" class="page-line"/>
          </svg>
        </div>
        <h2 class="nav-card__title">The Sessions</h2>
        <p class="nav-card__description">
          Ten accounts. Some nights we won. Some nights we barely survived.
          <em>The stones remember everything.</em>
        </p>
        <span class="nav-card__fragment">chronological / unredacted</span>
      </a>

      <a href="/reference" class="nav-card nav-card--warn">
        <div class="nav-card__sigil">
          <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2" class="star-points"/>
          </svg>
        </div>
        <h2 class="nav-card__title">The Index</h2>
        <p class="nav-card__description">
          Names. Places. Things that hunt us.
          <em>Knowledge is the only weapon that doesn't break.</em>
        </p>
        <span class="nav-card__fragment">entities / locations / threats</span>
      </a>
    </nav>

    <footer class="landing__footer">
      <p class="footer-quote">
        <span class="quote-mark">"</span>If the pulse breaks, the world will bleed.<span class="quote-mark">"</span>
      </p>
      <p class="footer-attribution">— recovered from Dhakaani ruins, depth unknown</p>
    </footer>
  </div>
</BaseLayout>

<style>
  @keyframes pulse-subtle {
    0%, 100% { opacity: 0.6; }
    50% { opacity: 1; }
  }

  @keyframes flicker {
    0%, 100% { opacity: 1; }
    92% { opacity: 1; }
    93% { opacity: 0.4; }
    94% { opacity: 1; }
    96% { opacity: 0.7; }
    97% { opacity: 1; }
  }

  @keyframes glow-soft {
    0%, 100% { filter: drop-shadow(0 0 2px currentColor); }
    50% { filter: drop-shadow(0 0 8px currentColor); }
  }

  @keyframes whisper-fade {
    0% { opacity: 0; transform: translateY(10px); }
    20% { opacity: 0.5; }
    80% { opacity: 0.5; }
    100% { opacity: 0; transform: translateY(-10px); }
  }

  .landing {
    min-height: 90vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: var(--space-xl);
    position: relative;
  }

  .landing::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(ellipse at center, transparent 0%, var(--color-bg-primary) 70%);
    pointer-events: none;
  }

  .landing__header {
    text-align: center;
    margin-bottom: var(--space-xl);
    position: relative;
  }

  .landing__whisper {
    font-family: var(--font-serif);
    font-style: italic;
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    letter-spacing: 0.3em;
    text-transform: lowercase;
    margin: 0 0 var(--space-md);
    animation: whisper-fade 8s ease-in-out infinite;
  }

  .landing__title {
    font-family: var(--font-display);
    font-size: clamp(2rem, 7vw, 3.5rem);
    background: linear-gradient(135deg, var(--color-gold) 0%, var(--color-gold-light) 50%, var(--color-gold) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin: 0 0 var(--space-md);
    line-height: 1.1;
    animation: flicker 12s ease-in-out infinite;
  }

  .landing__subtitle {
    font-family: var(--font-serif);
    font-size: var(--text-base);
    color: var(--color-text-muted);
    margin: 0;
    position: relative;
    min-height: 1.5em;
  }

  .subtitle-static {
    display: block;
  }

  .subtitle-pulse {
    display: block;
    font-style: italic;
    font-size: var(--text-sm);
    color: var(--color-gold);
    opacity: 0;
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    animation: pulse-subtle 6s ease-in-out infinite;
    animation-delay: 3s;
  }

  .landing__warning {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    padding: var(--space-md) var(--space-xl);
    margin-bottom: var(--space-2xl);
    border-top: 1px solid var(--color-border);
    border-bottom: 1px solid var(--color-border);
    max-width: 700px;
  }

  .warning-glyph {
    color: var(--color-gold);
    font-size: var(--text-lg);
    animation: glow-soft 3s ease-in-out infinite;
  }

  .warning-text {
    font-family: var(--font-serif);
    font-style: italic;
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    text-align: center;
    line-height: 1.6;
  }

  .landing__nav {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--space-xl);
    width: 100%;
    max-width: 1000px;
    margin-bottom: var(--space-2xl);
  }

  .nav-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: var(--space-2xl) var(--space-xl);
    background: var(--color-bg-secondary);
    border: 2px solid var(--color-border);
    border-radius: var(--radius-lg);
    text-decoration: none;
    color: var(--color-text);
    transition: all var(--transition-base);
    position: relative;
  }

  .nav-card::after {
    content: '';
    position: absolute;
    top: -1px;
    left: -1px;
    right: -1px;
    bottom: -1px;
    border-radius: var(--radius-lg);
    background: linear-gradient(135deg, transparent 0%, var(--color-gold) 50%, transparent 100%);
    opacity: 0;
    z-index: -1;
    transition: opacity var(--transition-base);
  }

  .nav-card:hover {
    border-color: var(--color-gold);
    transform: translateY(-4px);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
  }

  .nav-card:hover::after {
    opacity: 0.1;
  }

  .nav-card--active {
    border-color: var(--color-gold);
  }

  .nav-card--warn:hover {
    border-color: #a855f7;
  }

  .nav-card--warn:hover .nav-card__sigil {
    color: #a855f7;
  }

  .nav-card--warn:hover .nav-card__title {
    color: #a855f7;
  }

  .nav-card__sigil {
    color: var(--color-gold);
    margin-bottom: var(--space-lg);
    transition: all var(--transition-base);
  }

  .nav-card:hover .nav-card__sigil {
    transform: scale(1.05);
    animation: glow-soft 2s ease-in-out infinite;
  }

  .sigil-outer, .sigil-inner {
    transition: all var(--transition-base);
  }

  .nav-card:hover .sigil-inner {
    stroke-dasharray: 25;
    stroke-dashoffset: 50;
    animation: none;
  }

  .nav-card__title {
    font-family: var(--font-display);
    font-size: var(--text-xl);
    color: var(--color-text);
    margin: 0 0 var(--space-sm);
    transition: color var(--transition-fast);
    letter-spacing: 0.05em;
  }

  .nav-card:hover .nav-card__title {
    color: var(--color-gold);
  }

  .nav-card__description {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    margin: 0 0 var(--space-md);
    line-height: 1.7;
  }

  .nav-card__description em {
    display: block;
    margin-top: var(--space-xs);
    color: var(--color-gold);
    opacity: 0.8;
  }

  .nav-card__fragment {
    font-family: var(--font-mono, monospace);
    font-size: var(--text-xs);
    color: var(--color-text-muted);
    letter-spacing: 0.1em;
    text-transform: lowercase;
    opacity: 0.5;
    transition: opacity var(--transition-fast);
  }

  .nav-card:hover .nav-card__fragment {
    opacity: 0.8;
  }

  .landing__footer {
    text-align: center;
    padding-top: var(--space-xl);
    border-top: 1px solid var(--color-border);
    max-width: 500px;
  }

  .footer-quote {
    font-family: var(--font-serif);
    font-size: var(--text-lg);
    color: var(--color-text);
    margin: 0 0 var(--space-xs);
    line-height: 1.6;
  }

  .quote-mark {
    color: var(--color-gold);
    font-size: var(--text-2xl);
    line-height: 0;
    vertical-align: -0.2em;
  }

  .footer-attribution {
    font-family: var(--font-mono, monospace);
    font-size: var(--text-xs);
    color: var(--color-text-muted);
    margin: 0;
    letter-spacing: 0.05em;
    opacity: 0.6;
  }

  @media (max-width: 640px) {
    .landing {
      padding: var(--space-lg);
      min-height: auto;
    }

    .landing__nav {
      grid-template-columns: 1fr;
    }

    .nav-card {
      padding: var(--space-xl) var(--space-lg);
    }

    .landing__warning {
      flex-direction: column;
      padding: var(--space-lg);
    }

    .warning-glyph:first-child {
      display: none;
    }
  }
</style>
